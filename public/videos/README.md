# Product demo videos

Drop the walkthrough recordings here. The landing page picks them up
automatically — no code change needed.

    pasumaivelanmai-demo.mp4   (+ optional .vtt captions)
    pasumaitrade-demo.mp4      (+ optional .vtt captions)

Until a file exists, that product's player shows its poster with a
"Book a live walkthrough" call to action instead.

Specification, encoding command and what to record: see IMAGE-BRIEF.md, §6.
